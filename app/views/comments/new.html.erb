<%= form_with(model: @comment, url: user_post_comments_path, method: :post, scope: :comment) do |form| %>
        <div>
        <%= form.text_field :text, required: true, autofocus: true, placeholder: 'Create comment'%>
        <%= form.submit "Comment"%>
        <div>
    <%end%>